pub fn evaluate(file_contents: &str) -> i32 {
    let mut lines = 1;
    let mut chars = file_contents.chars().peekable();

    // while let Some(&char = chars.peek()) {
    //     match character {
    //         'a'..='z' | 'A'..='Z' => {
    //             let literals = [
    //                 'true', 'false', 'nil'
    //             ];
    //             let mut identifier = String::new();
    //             while let Some(&char) = chars.peek() {
    //                 if char.is_alphanumeric() {
    //                     identifier.push(char);
    //                     chars.next();
    //                 } else {
    //                     break;
    //                 }
    //             }
                
    //         },
    //     }
    // }
    0
}